#version 450

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

layout(set = 0, binding = 0, r8_snorm) uniform writeonly image2D uDst;

#define COPY_IMAGE_STORE(dst, texel) imageStore(uDst, dst, vec4((texel).x, 0.0, 0.0, 1.0))
#include "copy_image_iv_main.h"
